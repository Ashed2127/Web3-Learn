// BLOCK2---BLOCK2---BLOCK3---

const Blockchain = require("./proof");

//BLOCK 1
// --block headers(block number == timestamp == nonce == target difficulty == priv hash)
// -- block data == transaction data
// -- block hash == block hash


//properties of blockchain
//   -- no central server(decentralized)
//   -- distributed
// block1--block2---block3 on computer1 /node1
// block1--block2---block3 on computer2/node2 node2


// Smart contract is a code which run on blockchain
//    for solidity we use tools like:-
//             --hardhat: use for to write contarct, for compilation, to deploy the code on blockchain 
            // --fundert
            // --ganash(for beginner)
            // -- remix(online)

// solidity --> Blockchain deployment & interaction with app  